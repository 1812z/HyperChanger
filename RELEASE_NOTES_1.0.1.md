# HyperChanger v1.0.1

**HyperChanger 1.0.1 正式版发布！**

这是一个面向部分小米 HyperOS 4 Beta 系统的 LSPosed 自定义模块。本版本在通知、控制中心、锁屏、相机和系统设置外观定制的基础上，补充全局预设、设置设备信息卡片和作用域重启能力，并继续优化新版 System UI 重建后的 Hook 稳定性。

**新增**

* `Feature` 新增全局模块预设导入与导出，可将通知与控制中心、锁屏、相机、设备信息、设置外观和其他模块配置保存为 JSON，并在导入后同步到对应作用域。
* `Feature` 系统设置“我的设备”页新增2种自定义风格，支持自定义设备图片、Logo、更新卡片背景、文字、位置、间距、缩放和模糊效果。(样式1l来源: 酷安@Mr_Bocchi)
* `Feature` 新增锁屏数字键盘圆形背景材质，支持使用 HyperOS 4 新增的柔光玻璃材质。
* `Feature` 新增锁屏迷你播放器与系统媒体通知的切换功能，以及锁屏迷你音乐播放器的左右滑动切歌功能。

**变更与优化**

* `UI/UX` 更新全新应用图标。
* `UI/UX` "HyperOS 悬浮底栏"更新为最新的 HyperOS 4 底部导航栏风格，底部导航栏新增"图标+文本"自由组合的功能。
* `UI/UX` 锁屏设置中移除“显示 Lyricon 歌词”开关和“数字键盘行间距”滑块；对应底层功能和配置兼容逻辑保留不变。
* `Optimize` 在每个二级界面加入了独立的作用域应用重启入口。
* `Optimize` 优化锁屏迷你播放器的触摸交互和过渡效果。
* `Optimize` 优化隐藏锁屏底部文本功能，支持隐藏更多类型的文本，并支持自由组合。
* `Optimize` 优化锁屏媒体通知的显示模式，可选择不隐藏、始终隐藏或根据播放器状态动态显示。
* `Optimize` 完善通知、媒体通知、控制中心材质以及柔光玻璃导航的处理，减少视图重建和动态位置变化带来的渲染异常。

**修复与重构**

* `Fix` 修复在较新版本的 HyperOS 4 中"去除通知下沉限制"和"锁屏迷你音乐播放器"失效的问题。
* `Fix` 修复不开启锁屏快捷功能时锁屏迷你音乐播放器错位的问题。
* `Fix` 修复部分相机参数缺失时的兼容性问题，移除"徕卡一瞬"和"实况运镜"等某些机型的特有功能，以为不同 API 和系统构建增加保护性处理。
* `Refactor` 重构设备信息卡片、设置外观、锁屏媒体、通知材质和模块预设相关代码，降低各作用域之间的耦合。

---

## 问题反馈须知

欢迎提交 Bug 和改进建议。涉及应用崩溃、异常报错、白屏或功能失效时，请附上完整的 **Logcat 日志**、设备型号、Android/HyperOS 版本、启用的作用域和复现步骤。

> 🚨 **NO LOG, NO FIX.**
>
> 无日志、无复现步骤且只有一句描述的 Issue 将不予受理并直接关闭。

感谢每一位参与测试和反馈的用户，让 HyperChanger 变得更好！
