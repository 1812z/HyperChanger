# HyperChanger v1.0.0 Beta 2

**HyperChanger 1.0.0 Beta 2 发布！**

这是一个面向部分小米 HyperOS 4 Beta 系统的 LSPosed 自定义模块。本版本在首个 Beta 的通知、控制中心、锁屏和相机功能基础上，扩展了设置界面、背屏与媒体控制等场景，并提升了跨系统构建的兼容性。

**新增**

* `Experimental` 新增系统设置界面外观自定义，可替换设置首页与设备信息页的背景素材，调整卡片透明度、模糊和文字表现，并支持设置页 Logo 缩放与材质模式。
* `Feature` 新增设备信息展示配置，可自定义机型、处理器、内存、电池、屏幕、相机、系统版本、存储和硬件等信息，并可切换骁龙图标。
* `Experimental` 新增光栅壁纸服务，支持最多 4 个图片或视频素材、传感器倾斜切换、视频帧播放和灵敏度调节。
* `Feature` 新增背屏音乐控制应用白名单，可选择允许出现在背屏控制中的应用，并新增背屏与设置作用域。
* `Feature` 新增锁屏迷你播放器，可自定义显示、媒体通知隐藏、尺寸及纯色、高级材质和柔光玻璃背景，并适配锁屏自定义菜单避让。
* `Feature` 新增超级小爱输入法搜索页特殊外观开关，并加入 `com.xiaomi.type` 作用域。

**变更与优化**

* `UI/UX` 扩展通知中心、控制中心、音量面板、灵动岛、状态栏、锁屏快捷方式和息屏时钟的材质、模糊、透明度、圆角、尺寸与位置调节项。
* `Optimize` 支持应用全局主题后保持系统界面柔光玻璃效果，并解除息屏时钟“玻璃”和“叠加”材质的 OTA 转换限制。
* `Optimize` 重做状态栏尺寸设置的数据迁移，兼容旧版本的绝对值配置，并扩大可调范围。
* `Optimize` 统一设置同步与进程重启流程，新增设置页、背屏和超级小爱输入法的远程配置同步。

**修复与重构**

* `Fix` 修复部分设备不存在可选相机厂商参数时可能导致相机崩溃的问题，自动回退到 Camera2 兼容参数。
* `Fix` 修复锁屏媒体控制器失效或状态变化时可能重复刷新、遮挡自定义菜单的问题。
* `Fix` 修复控制中心磁贴圆角在部分布局下计算异常，导致圆角显示不一致的问题。
* `Refactor` 拆分设备信息、设置外观、背屏壁纸、音乐白名单和锁屏播放器模块，降低各作用域之间的耦合，并为厂商 API 增加保护性 Hook。
* `Refactor` 重构部分代码，优化 Hook 性能。

---

## 问题反馈须知

欢迎提交 Bug 和改进建议。涉及应用崩溃、异常报错、白屏或功能失效时，请附上完整的 **Logcat 日志**、设备型号、Android/HyperOS 版本、启用的作用域和复现步骤。

> 🚨 **NO LOG, NO FIX.**
>
> 无日志、无复现步骤且只有一句描述的 Issue 将不予受理并直接关闭。

感谢每一位参与测试和反馈的用户，让 HyperChanger 变得更好！

**APK SHA-256**

`AB49CDBD656809070405451977A386BD4F8D4479E08B36FEE1CDC55D326E22CB`
