# HyperChanger v1.1.0

**HyperChanger 1.1.0 正式版发布！**

这是一个面向部分小米 HyperOS 4 Beta 系统的 LSPosed 自定义模块。本版本在 1.0.1 的通知、控制中心、锁屏、相机和系统设置定制基础上，扩展锁屏组件、状态栏信号、超级小爱输入法与系统更新控制，并全面完善多语言和兼容性保护。

**新增**

* `Feature` 新增锁屏小组件，可组合天气、电量、日出日落、步数、闹钟、日程等信息卡片，支持拖拽排序、双组合布局、颜色/电量材质、通知避让以及自定义签名图片（PNG/SVG/矢量 XML）。
* `Feature` 新增状态栏双排信号与网络类型自定义，可调整缩放、位置、边距、显示逻辑，并支持隐藏指定信号图标和自定义网络制式文本。
* `Feature` 超级小爱输入法修改增强：解除黑名单词库、剪贴板、AI 与语音安全限制，并可调节键盘玻璃外观、颜色、圆角、透明度和模糊度。
* `Feature` 新增系统更新控制，可禁用系统更新、移除 OTA 限制，或自定义系统版本与 SOTA 版本显示。
* `Feature` 新增多语言功能，中英文/日文内置语言包、语言包导入导出与编辑功能；设置中加入 LICENSE、免责声明和 HyperOS 兼容性说明页面。

**变更与优化**

* `UI/UX` 统一使用柔光玻璃下拉菜单、弹窗和工具栏交互，优化动态背景、返回手势、页面切换与预览卡片表现。
* `UI/UX` 分类首页增加 System UI 连接状态提示；各功能页按作用域提供独立重启入口，未连接时自动禁用相关设置。
* `Optimize` 扩展设置设备信息页样式2风格卡片的图片、Logo、背景、文字、颜色和间距调节，并增强素材加载与刷新稳定性。
* `Optimize` 优化锁屏迷你播放器、导航栏材质、通知/控制中心玻璃效果及状态栏尺寸参数迁移，保留旧版本配置外观。
* `Optimize` 增加系统版本检测、免责声明确认码和调试预览流程，降低非 HyperOS 4 环境误启用 Hook 的风险。
* `Build` 项目许可证切换为 Apache License 2.0，并补充 NOTICE 与贡献指南说明。

**修复与重构**

* `Fix` 修复相机 6.7 等较新版本中默认焦段被配置文件覆盖、特殊能力检测失效的问题，并增加延迟重试和多版本反射保护。
* `Fix` 修复相册媒体编辑器进程不是首个包初始化时水印 Hook 未安装的问题。
* `Fix` 修复锁屏组件组合迁移、媒体播放器图片圆角、状态栏缩放值和设置外观素材同步等边界情况。
* `Refactor` 重构 System UI、设置外观、锁屏组件、超级小爱和相机 Hook 的配置读取与作用域处理，减少系统重建及不同厂商构建导致的失效。

---

## 问题反馈须知

欢迎提交 Bug 和改进建议。涉及应用崩溃、异常报错、白屏或功能失效时，请附上完整的 **Logcat 日志**、设备型号、Android/HyperOS 版本、启用的作用域和复现步骤。

> 🚨 **NO LOG, NO FIX.**
>
> 无日志、无复现步骤且只有一句描述的 Issue 将不予受理并直接关闭。

感谢每一位参与测试和反馈的用户，让 HyperChanger 变得更好！

**APK SHA-256**

`4D280C61FA1678EA9821254088238DFBE007A7901C00F76B3A1703588D7F0B24`
